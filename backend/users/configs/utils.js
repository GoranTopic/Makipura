const allowedPropertiesInRequest = { 
		// this is a list of all the properties 
		// that are allowed to be passed in a request body
		username: true,
		lastname: true,
		firstname: true,
		mobileNumber: true,
		description: true,
		password: true,
		email: true,
		profileImage: true,
		backgroundImage: true,
}

export { allowedPropertiesInRequest }
