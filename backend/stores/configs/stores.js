/* This is file for changin global configuration settings */

const allowedConditions = [ "brand new", "new", "slightly used", "used", "worn", "damaged" ]

const allowedCurrencies = [ 'USD', ]

export { allowedConditions, allowedCurrencies };
